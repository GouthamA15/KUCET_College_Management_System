import { db } from '@/db'; import { staffAcademicAffiliations, academicPrograms, auditLogs, facultyHodAssignments } from '@/db/schema'; import { eq, and } from 'drizzle-orm'; import { apiResponse, apiError, wrapHandler } from '@/lib/api-utils'; import { z } from 'zod'; export const GET = wrapHandler({ auth: 'hod', handler: async (req, { user }) => { const hodAssignment = await db.query.facultyHodAssignments.findFirst({ where: and(eq(facultyHodAssignments.staff_account_id, user.id), eq(facultyHodAssignments.is_active, true)) }); if (!hodAssignment) return apiError('Not an active HOD', 403); const allPrograms = await db.select({ id: academicPrograms.id, program_code: academicPrograms.program_code, program_name: academicPrograms.program_name }).from(academicPrograms); const facultyAffiliations = await db.select({ program_id: staffAcademicAffiliations.program_id }).from(staffAcademicAffiliations).where(eq(staffAcademicAffiliations.staff_account_id, user.id)); const allocatedProgramIds = facultyAffiliations.map(a => a.program_id).filter(id => id !== null); return apiResponse({ data: { allPrograms, allocatedProgramIds } }); } }); const patchSchema = z.object({ programChanges: z.array(z.object({ programId: z.number().int().positive(), enabled: z.boolean() })) }); export const PATCH = wrapHandler({ auth: 'hod', schema: patchSchema, handler: async (req, { user, data }) => { const hodAssignment = await db.query.facultyHodAssignments.findFirst({ where: and(eq(facultyHodAssignments.staff_account_id, user.id), eq(facultyHodAssignments.is_active, true)) }); if (!hodAssignment) return apiError('Not an active HOD', 403); await db.transaction(async (tx) => { const baseAffiliation = await tx.select({ department_id: staffAcademicAffiliations.department_id }).from(staffAcademicAffiliations).where(eq(staffAcademicAffiliations.staff_account_id, user.id)).limit(1); if (baseAffiliation.length === 0) throw new Error('Base department not found'); const deptId = baseAffiliation[0].department_id; for (const change of data.programChanges) { if (change.enabled) { const existing = await tx.select().from(staffAcademicAffiliations).where(and(eq(staffAcademicAffiliations.staff_account_id, user.id), eq(staffAcademicAffiliations.program_id, change.programId))).limit(1); if (existing.length === 0) { await tx.insert(staffAcademicAffiliations).values({ staff_account_id: user.id, department_id: deptId, program_id: change.programId }); } } else { await tx.delete(staffAcademicAffiliations).where(and(eq(staffAcademicAffiliations.staff_account_id, user.id), eq(staffAcademicAffiliations.program_id, change.programId))); } } await tx.insert(auditLogs).values({ action: 'HOD_SELF_PROGRAM_ALLOCATION_UPDATED', user_id: user.id, user_type: 'staff', target_id: user.id.toString(), target_type: 'staff_academic_affiliations', payload_after: { program_changes: data.programChanges }, ip_address: req.headers.get('x-forwarded-for') || '127.0.0.1' }); }); return apiResponse({ message: 'Programs updated successfully' }); } });
