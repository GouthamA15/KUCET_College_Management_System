import React from "react";
import { Document, Page, Text } from "@react-pdf/renderer";

// Placeholder template to satisfy imports and avoid module-not-found errors.
// Replace with the full design when ready.
export default function CourseCompletionCertificatePDF() {
	return (
		<Document>
			<Page>
				<Text>Course Completion Certificate — Placeholder</Text>
			</Page>
		</Document>
	);
}
