name: Auto Deploy to Self-Hosted Server

on:
  push:
    branches:
      - main

jobs:
  deploy-self-hosted:
    name: Build & Deploy on Self-Hosted Desktop
    runs-on: self-hosted
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Load Server Environment
        run: |
          if [ -f /home/kucet-dev/.env ]; then
            cp /home/kucet-dev/.env .env
          fi

      - name: Deploy Production Application
        run: |
          npm ci --only=production || npm install
          npm run db:migrate
          npm run build
          if command -v pm2 &> /dev/null; then
            pm2 reload kucet-cms || pm2 start npm --name "kucet-cms" -- start
          else
            npm run start &
          fi
